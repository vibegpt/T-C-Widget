[phases.setup]
nixPkgs = ["nodejs_18", "npm-9_x"]

[phases.install]
cmds = ["cd web && npm install"]

[phases.build]
cmds = ["cd web/frontend && npm install && npm run build"]

[start]
cmd = "cd web && npm start"
